import {
    Container,
    Header,
} from "../styles/components/Sidebar";

function Sidebar() {
    return (
        <Container>
            <Header>
                User name
            </Header>

            rooms
        </Container>
    );
};

export default Sidebar;
